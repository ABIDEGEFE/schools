# Legacy duplicate views removed — all API logic consolidated into core.views.ViewSets and actions.
# If you need the old APIViews for compatibility, re-create them in core/views.py as thin adapters that call the ViewSets.



